import styled from "styled-components/native";
import theme from "../../styles/theme";

export const TabComponentContainer = styled.View`
  justify-content: flex-end;
`;
export const TabComponentContent = styled.View`
 background :  ${theme.colors.slate};
border-radius: 6px;
 width : 100%;
 height : 50px;
`;